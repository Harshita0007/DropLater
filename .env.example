# API Configuration
PORT=3000
NODE_ENV=development

# Database (use service name for Docker)
MONGODB_URI=mongodb://mongo:27017/droplater

# Redis (use service name for Docker)
REDIS_URL=redis://redis:6379

# Authentication
ADMIN_TOKEN=super-secret-admin-token-2024

# Webhook configuration (use service name for Docker)
WEBHOOK_SINK_URL=http://sink:4000/sink

# Worker configuration
WORKER_POLL_INTERVAL=5000